const bidSum = 80;
const clock = 5;
const wordClock = 40;
const dey = 11

const sum = (wordClock / clock) * 80
console.log(`${sum}$`)
const world = (wordClock / clock) < 11;
console.log(`Возьмусь за роботу ${world}` )
